<% if not @auction.errors.empty? %>
  $("#error_explanation").html("<%= escape_javascript(render('errors', :auction => @auction)) %>");
  $("#error_explanation").show();
<% else %>
  document.location = "<%= escape_javascript(edit_auction_path(@auction)) %>";
<% end %>